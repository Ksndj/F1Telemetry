<UserControl x:Class="F1Telemetry.App.Views.ChartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:controls="clr-namespace:F1Telemetry.App.Views.Controls"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="680"
             d:DesignWidth="1120">
    <ScrollViewer x:Name="ChartsScrollViewer"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Border Padding="20"
                CornerRadius="20"
                Background="#0F1B2E">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="18" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="White"
                               Text="实时图表" />
                    <TextBlock Margin="0,6,0,0"
                               Foreground="#9AB0C9"
                               Text="当前圈图表按 250ms 刷新，多圈趋势只在新圈生成后更新。"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="320" />
                        <RowDefinition Height="14" />
                        <RowDefinition Height="320" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <controls:TelemetryChartControl DataContext="{Binding SpeedChartPanel}" />
                    <controls:TelemetryChartControl Grid.Column="2"
                                                    DataContext="{Binding InputsChartPanel}" />
                    <controls:TelemetryChartControl Grid.Row="2"
                                                    DataContext="{Binding FuelTrendChartPanel}" />
                    <controls:TelemetryChartControl Grid.Row="2"
                                                    Grid.Column="2"
                                                    DataContext="{Binding TyreWearTrendChartPanel}" />
                </Grid>
            </Grid>
        </Border>
    </ScrollViewer>
</UserControl>
