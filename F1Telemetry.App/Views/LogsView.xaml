<UserControl x:Class="F1Telemetry.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="680"
             d:DesignWidth="1120">
    <ScrollViewer x:Name="LogsScrollViewer"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Border Padding="20"
                CornerRadius="20"
                Background="#0F1B2E">
            <StackPanel>
                <TextBlock FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="White"
                           Text="事件日志" />
                <TextBlock Margin="0,6,0,0"
                           Foreground="#9AB0C9"
                           Text="完整日志按 System / UDP / RaceEvent / AI / TTS / Storage 分类显示。"
                           TextWrapping="Wrap" />

                <ItemsControl Margin="0,18,0,0"
                              ItemsSource="{Binding LogEntries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,8"
                                    Padding="12"
                                    CornerRadius="12"
                                    Background="#132743">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70" />
                                        <ColumnDefinition Width="54" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Foreground="#7FA5D8" Text="{Binding Timestamp}" />
                                    <TextBlock Grid.Column="1" Foreground="#D7E4F3" Text="{Binding Category}" />
                                    <TextBlock Grid.Column="2"
                                               Foreground="White"
                                               Text="{Binding Message}"
                                               TextWrapping="Wrap" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>
